<UserControl x:Class="Database_Hub.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Database_Hub.Views"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="#F5F5F5">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Orientation="Vertical" Margin="20">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,30">
                <TextBlock Text="Database Hub" FontSize="32" FontWeight="Bold" VerticalAlignment="Center"/>
                <TextBlock Text="🗄️" FontSize="32" Margin="10,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,30">
                <!-- Connect with Database Card -->
                <Border Background="White" CornerRadius="10" Margin="10" Padding="10" Width="300" Height="200"
                        BorderBrush="#E0E0E0" BorderThickness="1">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="5" ShadowDepth="2" Color="#CCCCCC"/>
                    </Border.Effect>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="🔗" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                        <TextBlock Text="Connect with Database" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                        <TextBlock Text="Authenticate login and connect to database server" 
                                TextWrapping="Wrap" HorizontalAlignment="Center" Margin="0,0,0,20" Foreground="#666666"/>
                        <!-- <Button Content="Connect" HorizontalAlignment="Center" Padding="20,10" Background="#0078D4" Foreground="White" 
                                BorderThickness="0" FontSize="14" Command="{Binding ConnectCommand}"/> -->

                        <Button Width="120"
                                Height="35"
                                HorizontalAlignment="Center"
                                Background="#0078D4"
                                Foreground="White"
                                BorderThickness="1"
                                BorderBrush="White"
                                FontSize="15"
                                Command="{Binding ConnectCommand}">

                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="15"
                                            Padding="5">

                                        <StackPanel Orientation="Horizontal"
                                                    HorizontalAlignment="Center"
                                                    VerticalAlignment="Center">
                                            <!-- Text -->
                                            <TextBlock Text="Connect"
                                                    Foreground="{TemplateBinding Foreground}"
                                                    VerticalAlignment="Center"/>
                                            <!-- Space -->
                                            <TextBlock Width="6"/>
                                            <!-- Icon -->
                                            <TextBlock Text="🔗"
                                                    FontSize="16"
                                                    Foreground="{TemplateBinding Foreground}"
                                                    VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </StackPanel>
                </Border>

                <!-- Guest Mode Card -->
                <!-- <Border Background="White" CornerRadius="10" Margin="10" Padding="10" Width="300" Height="200"
                        BorderBrush="#E0E0E0" BorderThickness="1">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="5" ShadowDepth="2" Color="#CCCCCC"/>
                    </Border.Effect>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="👤" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                        <TextBlock Text="Guest Mode" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                        <TextBlock Text="Access limited features without database connection" 
                                TextWrapping="Wrap" HorizontalAlignment="Center" Margin="0,0,0,20" Foreground="#666666"/>

                        <Button Width="170"
                                Height="35"
                                HorizontalAlignment="Center"
                                Background="#107C10"
                                Foreground="White"
                                BorderThickness="1"
                                BorderBrush="White"
                                FontSize="15"
                                Command="{Binding GuestCommand}">

                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="15"
                                            Padding="5">

                                        <StackPanel Orientation="Horizontal"
                                                    HorizontalAlignment="Center"
                                                    VerticalAlignment="Center">
                                            <TextBlock Text="Continue as Guest"
                                                    Foreground="{TemplateBinding Foreground}"
                                                    VerticalAlignment="Center"/>
                                            <TextBlock Width="6"/>
                                            <TextBlock Text="🕶️"
                                                    FontSize="16"
                                                    Foreground="{TemplateBinding Foreground}"
                                                    VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </StackPanel>
                </Border> -->
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>