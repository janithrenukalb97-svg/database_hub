<Window x:Class="Database_Hub.Views.Common.ConfirmActionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterScreen"
        Background="Transparent"
        Title="Confirm">
    <Border CornerRadius="16" BorderBrush="#F5C2C7" BorderThickness="1" Background="White" Padding="0">
        <Grid MinWidth="430" MaxWidth="560">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="#B42318" CornerRadius="16,16,0,0" Padding="16,12" MouseLeftButtonDown="Header_MouseLeftButtonDown">
                <TextBlock x:Name="TitleTextBlock" Text="Confirm" FontSize="17" FontWeight="SemiBold" Foreground="White"/>
            </Border>

            <Grid Grid.Row="1" Margin="18,16,18,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Width="34" Height="34" Background="#FEF3F2" CornerRadius="17" VerticalAlignment="Top">
                    <TextBlock Text="?" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="20" FontWeight="Bold" Foreground="#B42318"/>
                </Border>

                <TextBlock x:Name="MessageTextBlock"
                           Grid.Column="2"
                           TextWrapping="Wrap"
                           VerticalAlignment="Top"
                           FontSize="14"
                           Foreground="#1B1F23"/>
            </Grid>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,14,14">
                <Button Content="Cancel"
                        Width="96"
                        Height="34"
                        Click="OnCancelClick"
                        Background="#475467"
                        Foreground="White"
                        BorderBrush="#475467"
                        BorderThickness="1"
                        FontWeight="SemiBold"
                        Margin="0,0,8,0">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="10">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <Button Content="Delete"
                        Width="96"
                        Height="34"
                        Click="OnConfirmClick"
                        Background="#B42318"
                        Foreground="White"
                        BorderBrush="#B42318"
                        BorderThickness="1"
                        FontWeight="SemiBold">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="10">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>
