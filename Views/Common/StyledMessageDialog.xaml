<Window x:Class="Database_Hub.Views.Common.StyledMessageDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterScreen"
        Background="Transparent"
        Title="Message">
    <Border CornerRadius="16" BorderBrush="#E6E9EF" BorderThickness="1" Background="White" Padding="0">
        <Grid x:Name="DialogContentRoot" MinWidth="420" MaxWidth="560">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="#C23934" CornerRadius="16,16,0,0" Padding="16,12">
                <TextBlock x:Name="TitleTextBlock"
                           Text="Error"
                           FontSize="17"
                           FontWeight="SemiBold"
                           Foreground="White"/>
            </Border>

            <Grid Grid.Row="1" Margin="18,16,18,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Width="34" Height="34" Background="#FDEBEC" CornerRadius="17" VerticalAlignment="Top">
                    <TextBlock Text="!" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="20" FontWeight="Bold" Foreground="#B42318"/>
                </Border>

                <TextBlock x:Name="MessageTextBlock"
                           Grid.Column="2"
                           TextWrapping="Wrap"
                           VerticalAlignment="Top"
                           FontSize="14"
                           Foreground="#1B1F23"/>
            </Grid>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,14,14">
                <Button Content="OK"
                        Width="92"
                        Height="34"
                        Click="OnOkClick"
                        Background="#C23934"
                        Foreground="White"
                        BorderBrush="#C23934"
                        BorderThickness="1"
                        FontWeight="SemiBold">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="10">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>
